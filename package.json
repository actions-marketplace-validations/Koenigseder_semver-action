{
  "name": "semver-action",
  "version": "1.0.0",
  "description": "A GitHub Action which automatically creates releases and semver tags",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "prestart": "ncc build src/index.ts",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "@vercel/ncc": "^0.36.1",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  }
}
